/**
 * Interface representing an image.
 * 
 * @interface Iimage
 * @property {string} image - The image URL.
 */
// interfaces/IimageBlob.ts
export interface Iimage {
    file: any, // Change {} to any to store the actual file object
    name: string,
} 
